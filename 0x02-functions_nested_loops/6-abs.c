#include "main.h"

/**
 * _abs - return absolute number
 * @int: number to be checked
 *
 * Return: Always 0 (Success)
 */

int _abs(int)
{
	if (int > 0)
		_putchar(int);
	else if (int < 0)
		_putchar(0 - int);
	else
		_putchar(0);
	return (0);

}

#include "main.h"

/**
 * _abs - return absolute number
 * @n: number to be checked
 *
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
		_putchar(-n);
	else
		_putchar(0);
	return (0);

}
